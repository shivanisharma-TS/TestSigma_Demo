{
  "name": "weekly-schedule-bot",
  "version": "1.0.0",
  "description": "Slack bot — posts weekly topic schedules every Friday",
  "type": "module",
  "main": "server.js",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
